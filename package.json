{
  "name": "nani-mono-da",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace packages/server",
    "dev:web": "npm run dev --workspace packages/web",
    "dev:fleet-server": "npm run dev --workspace packages/fleet-server",
    "dev:fleet-web": "npm run dev --workspace packages/fleet-web",
    "build": "npm run build --workspaces --if-present"
  }
}
